/**
 * VocalIA Remotion Entry Point
 *
 * Registers all video compositions for VocalIA marketing content.
 */
import { registerRoot } from 'remotion';
import { Root } from './Root';

registerRoot(Root);
